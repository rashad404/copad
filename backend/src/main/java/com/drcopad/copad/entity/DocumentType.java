package com.drcopad.copad.entity;

/** What a document is, which decides how it is read and where it appears. */
public enum DocumentType {
    LAB_RESULT,
    IMAGING,
    PRESCRIPTION,
    DISCHARGE_SUMMARY,
    REFERRAL,
    VACCINATION,
    INSURANCE,
    OTHER
}
