{
  "name": "copad-public-server",
  "version": "1.0.0",
  "description": "Static file server for public_html during local development",
  "main": "serve-public.js",
  "scripts": {
    "serve-public": "node serve-public.js",
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && ./run-local.sh\" \"cd next-frontend && npm run dev\"",
    "dev:backend": "cd backend && ./run-local.sh",
    "dev:frontend": "cd next-frontend && npm run dev",
    "seed": "cd backend && ./seed-local.sh"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
